module.exports = {
  output: 'export', // Habilita la exportación estática
  // Opcional: Cambia la carpeta de salida (por defecto es 'out')
  distDir: 'dist', // Si quieres que los archivos se guarden en /dist en lugar de /out
};